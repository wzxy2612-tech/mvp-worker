{
  // 讓測試檔認得 cloudflare:workers / cloudflare:test 的型別
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": [
      "@cloudflare/workers-types",
      "@cloudflare/vitest-pool-workers/types"
    ]
  },
  "include": ["./**/*.ts", "../src/**/*.ts"]
}
